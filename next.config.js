/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
    server: {
      port: 3001, // Replace 3001 with the desired port number
    },
  images: {

    formats: ['image/webp']
  },
  };